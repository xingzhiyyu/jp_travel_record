# 可直接复制给 AI 助手的提示词

你在一个旅行地图视频项目根目录工作。请先完整阅读 AGENTS.md、README.md、docs/WORKFLOW.md，
再检查实际代码和命令帮助，按现有能力执行，不要虚构参数或默认旧聊天中的建议已实现。

我的输入是 record.txt，可能来自“生成器/”里的导出，也可能是手写的旅行记录。
请保留原件，清洗到 record.cleaned.txt，并在 output/cleaning-report.md 逐项记录修改理由、
原始行号、地理核对证据和待确认项。保留顺序、交通方式、日夜标记。
注意 JR/私铁同名站、站点与岛屿/景点的混淆、实际支线方向、跨城段缩放和港区海陆边界。
只有上下文唯一明确时才补缺失端点；涉及真实行程选择的歧义向我提问，同时继续其他检查。

安装并验证本地依赖，先做纯语法检查，然后 --resolve-only。检查解析 JSON 中真实线路几何、
异常折返、换乘断点、端点偏移和直线回退，不把“能解析”当作“地理正确”。
可以联网读取公开 OSM/路由数据核实，复用缓存并控制请求量；密钥只通过环境变量使用。
不要把生成器站表的排列直接当成真实轨道路径。
新地区缺精细海岸线时，按 docs/WORKFLOW.md 的 2.5 节和 docs/COASTLINE_PROMPT.md，
使用 coastline 命令分区获取、验证、注册，再检查真实渲染帧；不要把下载成功等同于视觉验证。

先生成小型预览和关键帧，重点检查换乘、日夜切换、大吉山步行、宇治、江之岛、海芝浦、
长铁路段和结尾。如本次输入不涉及这些地方，选择本次类似场景。能够看图就实际查看；
不能看图时交付图片并明确这部分未验证。报告哪些修复经过数据核实、哪些仅为视觉调整。

本轮先做到清洗、解析、可审阅预览。若我明确要求完整版，就继续渲染至正式 MP4 完成并验证，
按机器小样性能选择 workers；只报告有依据的耗时范围，不按分片文件大小猜进度。
不要未经请求发布 GitHub、推送、上传我的行程、缓存或视频。交付简洁说明和实际文件路径。
